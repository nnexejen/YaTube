image:
  repository: buv12/yatube
  tag: 1.2
  pullPolicy: Always

backend:
  replicas: 1
  env:
    DEBUG: false
    SECRET_KEY: p&l%385148kslhtyn^##a1)ilz@4zqj=rq&agdol^##zgl9(vs   
    ALLOWED_HOSTS: ".yatube.local,localhost,127.0.0.1"
  dbHost: postgres
  dbPort: 5432
  dbName: postgres
  staticStorage: 1Gi
  mediaStorage: 1Gi
  postgresStorage: 1Gi

postgres:
  image: postgres:13.0-alpine
  password: postgres
  user: postgres
  database: postgres
  host: "postgres"

nginx:
  image: nginx:1.21.3-alpine

ingress:
  enabled: true
  host: yatube.local
  tls:
    enabled: true
    secretName: yatube-tls
    hosts:
      - yatube.local

service:
  type: ClusterIP
  port: 80
  targetPort: 80    

superuser:
  username: admin
  email: admin@example.com
  password: strongpassword123  

test:
  enabled: false      

tls:
  crtFile: "ssl/tls.crt"  
  keyFile: "ssl/tls.key"
  